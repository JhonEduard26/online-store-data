const { models } = require('../libs/sequelize');

class OrderService {
  async create(data) {
    const rta = '';
    return rta;
  }

  async find() {
    const rta = '';
    return rta.rows;
  }

  async findOne(id) {
    const rta = '';
    return rta;
  }

  async update(id, changes) {
    const rta = '';
    return rta;
  }

  async delete(id) {
    const rta = '';
    return rta;
  }
}

module.exports = OrderService;
